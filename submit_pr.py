import urllib.request, json
import sys

def submit():
    pass

# I'm going to just use the message_user tool since the submit tool isn't explicitly defined as a python or bash command and might be an internal function.
